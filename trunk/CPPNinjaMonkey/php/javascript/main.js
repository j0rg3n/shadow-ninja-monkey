var game;

function init() {
}

function commit_Click(element) {
	/*
    $('commit').update("[Committing...]");
    var state = game.getState();
    rpc.beginCall('commitBoard', state.getPlayerPieces(initialState.playerID), function(result, error) {
        if (!error) {
            $('commit').update('[committed.]');
            game.setState(new GameState(result));
        } else {
            $('commit').update('[commit failed: ' + error + ']');
        }
    });
	*/
}

